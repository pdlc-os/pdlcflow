// Phase A stub. Will host interaction-mode, voice (future), tenant overrides.

export function SettingsDrawer() {
  return (
    <aside className="w-72 border-l border-border p-4 text-sm">
      <h3 className="mb-3 font-medium">Settings</h3>
      <p className="text-muted-fg">Lands in Phase B (per-project + per-user preferences).</p>
    </aside>
  );
}
